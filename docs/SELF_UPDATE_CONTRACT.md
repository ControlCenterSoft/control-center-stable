# Self-update contract

Source of truth: latest non-prerelease GitHub release of `ControlCenterSoft/control-center-stable` as advertised by the repository VERSION and immutable release assets. Check cadence: every 10 minutes. Update must fail closed on checksum, backup, migration, start or health failure.
