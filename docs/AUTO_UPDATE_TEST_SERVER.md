# Test server auto-update policy

The dedicated Control Center test server is intended to follow the newest published stable release. The canonical check interval is 10 minutes via `control-center-auto-update.timer`. The canonical updater is `scripts/stable-auto-update.sh`.
